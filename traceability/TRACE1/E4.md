# TRACE1 -- E4

**Scenario:** E4 — the .feature files' inline evidence comments are cleaned out, scenarios unchanged
**Feature file:** `features/TRACE1-traceability-store.feature`

## Evidence

```
Evidence: `grep -rl '# Evidence' features/` returns nothing. Scenario count confirmed
  identical before and after (178 both times) by independently counting `Scenario:`
  lines in the pre- and post-migration git trees.

Round 2 (TRACE1 covering itself, commit 0e48260): re-ran the same check after
  features/TRACE1-traceability-store.feature's own inline evidence was migrated --
  `grep -rn '^\s*#\s*Evidence:' features/` still returns nothing (the one surviving
  hit for the bare string "# Evidence" is E4's own Then-clause prose quoting it, not
  a real comment). Scenario count for that file unchanged at 6 before and after.
```
